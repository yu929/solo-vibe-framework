# 设计准入评审报告

## 冻结输入
- 需求版本: `<...>` · 设计版本: `技术设计 vX.Y / 实施计划 vX.Y` · 工程基线: `<...>`
- 评审清单: `<轨专属 reviewer 路径>`
- 评审范围: `<...>` · 非范围: `<...>`

## 总览
- 检查文档数: `N`
- 本轮发现: P0×`a` · P1×`b` · P2×`c` · P3×`d`
- 与台账对比: 新增 `m` 条；命中已 REJECTED/DEFERRED 而跳过 `k` 条
- subagent 处理（如启用）: 原始 `x` 条 → 二次核实保留 `y` 条 → 过滤误报/重复 `z` 条

## P0 · 必须修改（编码前清零）
| id | 文档·章节 | 问题 | 证据(原文) | 触发 / 后果 | 建议 |
|----|----------|------|-----------|------------|------|

## P1 · 需明确决策（改 / DEFER / 接受风险，三选一）
| id | 文档·章节 | 问题 | 证据 | 触发 / 后果 | 决策 |
|----|----------|------|------|------------|------|

## P2 / P3 · backlog / 暂不（含"海拔过低、建议下放编码"）
| id | 文档·章节 | 问题 | 处置 |
|----|----------|------|------|

## 准入结论
- [ ] ✅ **可开工** —— 满足停止规则：Accepted P0 清零、P1 有决策、连续一轮无新 Accepted P0/P1、至少一个垂直切片可端到端验证。
- [ ] ⛔ **待修后复审** —— 开工前必做：
  1. `<P0/P1 修复项>`
  - 修复后只跑**一次复审**（不重启完整多轮）；之后即便仍有 P2/P3 也开工。

> 台账已更新：`<调用方指定的 ledger 路径>`。
