# 设计准入评审报告

> 这份中文报告供人做开工判断。
> `references/reviewer-checklist.md` 中的英文格式由 reviewer 交给主 agent，属于中间产物。

## 冻结输入
- 需求版本：`<...>` · 设计版本：`技术设计 vX.Y / 实施计划 vX.Y` · 工程基线：`<...>`
- 评审清单：`<轨专属 reviewer 路径>`
- 评审范围：`<...>` · 不评审：`<...>`

## 总览
- 检查文档数：`N`
- 本轮发现：P0×`a` · P1×`b` · P2×`c` · P3×`d`
- 与台账对比：新增 `m` 条；另有 `k` 条因已标记 REJECTED/DEFERRED 而跳过
- subagent 处理（如启用）：收到 `x` 条，二次核实后保留 `y` 条，过滤误报或重复 `z` 条

## P0 · 开工前必须修改

| id | 文档·章节 | 问题 | 原文证据 | 触发条件 / 后果 | 建议 |
|----|----------|------|-----------|------------|------|

## P1 · 需要明确决策

每条选择修改、DEFER 或接受风险。

| id | 文档·章节 | 问题 | 证据 | 触发 / 后果 | 决策 |
|----|----------|------|------|------------|------|

## P2 / P3 · 放进 backlog 或暂不处理

包括海拔过低、适合留到编码时处理的问题。

| id | 文档·章节 | 问题 | 处置 |
|----|----------|------|------|

## 准入结论
- [ ] 可开工：Accepted P0 已清零，P1 均有决策，连续一轮没有新增 Accepted P0/P1，且至少一个垂直切片可以端到端验证。
- [ ] 待修后复审。开工前完成：
  - `<P0/P1 修复项>`
  - 修复后只复审一次，不重启完整多轮；即使仍有 P2/P3，也按停止规则开工。

> 台账已更新：`<调用方指定的 ledger 路径>`。
