# 设计准入评审报告

> 这份中文报告供人做开工判断。
> `references/reviewer-checklist.md` 中的英文格式由 reviewer 交给主 agent，属于中间产物。

## 冻结输入
- 需求版本：`<...>` · 设计版本：`技术设计 vX.Y / 实施计划 vX.Y` · 工程基线：`<...>`
- 评审清单：`<轨专属 reviewer 路径>`
- 评审范围：`<...>` · 不评审：`<...>`

## 总览
- 检查文档数：`N`
- 本轮发现：P0×`a` · P1×`b` · P2×`c` · P3×`d`
- 与台账对比：新增 `m` 条；另有 `k` 条因已标记 REJECTED/DEFERRED 而跳过
- subagent 处理（如启用）：收到 `x` 条，二次核实后保留 `y` 条，过滤误报或重复 `z` 条

## P0 · 开工前必须修改

| id | 文档·章节 | 问题 | 原文证据 | 触发条件 / 后果 | 建议 |
|----|----------|------|-----------|------------|------|

## P1 · 需要明确决策

每条选择修改、DEFER 或接受风险。

| id | 文档·章节 | 问题 | 证据 | 触发 / 后果 | 决策 |
|----|----------|------|------|------------|------|

## P2 / P3 · 放进 backlog 或暂不处理

包括海拔过低、适合留到编码时处理的问题。

| id | 文档·章节 | 问题 | 处置 |
|----|----------|------|------|

## 准入结论

满足以下三条才可开工：

1. Accepted P0 已清零。
2. 每条 Accepted P1 都有明确处置：已修复、`DEFERRED` 并带 `reopen_condition`，或 `ACCEPTED_RISK` 并写明理由和责任人。
3. 至少一个垂直切片能端到端验证。

根据评审如何结束，勾选一项：

- [ ] **可开工 · 已收敛**：最后一轮完整评审没有新增 Accepted P0/P1，新发现只剩 P2/P3、重复、无证据或超范围。
- [ ] **可开工 · 预算用尽**：两轮已用完，仍有 `n` 条 Accepted P1 通过明确处置结案，逐条见上面 P1 表的「决策」列。不开第三轮。
- [ ] **不可开工**：仍有未清零的 Accepted P0。开工前完成：
  - `<P0 修复项>`
  - 修复后只复核这几条，不重启完整评审；复核不计入两轮预算。

> 台账已更新：`<调用方指定的 ledger 路径>`。
