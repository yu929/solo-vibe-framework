# 切片地图

> 这份文件回答两件事：**这一阶段做完用户能完整做成什么**，以及**下一片做哪个**。
> 需求全量在完整 PRD，术语在 `CONTEXT.md`，有取舍的决定在 `docs/adr/`，界面结构在高保真定稿。这里都不重复。

## 1. 当前阶段目标

<!--
一条端到端的用户价值，不是一批功能。
好："用户能配置一个数据源并跑通一次同步"
坏："做完数据源、同步、日志三个模块"

判据：说不出「用户从哪进来、做完什么、看到什么结果」，那它还是一批功能。
-->

## 2. 切片清单

<!--
四列都要填。填不满的多半不是一片。

- 阻塞边：只写**真的挡住它**的片。顺序不是依赖
- 端到端能验证什么：一句话，用户视角。说不出来就不是切片
- 本片对应的高保真屏：定稿里的具体路径。这一列是进 task 时填 implement.jsonl 的取值范围，
  空着就等于让实现期的子 agent 自由发挥
-->

| # | 切片 | 阻塞边 | 端到端能验证什么 | 本片对应的高保真屏 |
|---|---|---|---|---|
| 1 |  | 无 |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |

<!-- 宽重构（影响面铺满全仓的机械改动）走 expand–contract，第四列通常为空。见 skill 的 references/wide-refactor.md -->

## 3. frontier

<!--
frontier = 阻塞边已全部完成的片，也就是**现在就可以开工的**。
每片做完更新一次。从上往下数会漏掉那些其实早就能开工的片。
-->

**现在可开工**：

- [ ] #_ ——

**在等**：

| 片 | 在等谁 |
|---|---|
|  |  |

**已完成**：

- [x] #_ ——

---

## 待确认

<!-- Quiz 两轮之后仍有分歧的记这里，别进第三轮。四字段：现象 / 判定 / 动作 / 落点 -->

| 现象 | 判定 | 动作 | 落点 |
|---|---|---|---|
|  |  |  |  |
