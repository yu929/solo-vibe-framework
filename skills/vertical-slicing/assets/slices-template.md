# 切片地图

> 这份文件只回答两个问题：这一阶段结束时用户能完整做成什么，下一片做什么。
> 完整需求留在 PRD，术语留在 `CONTEXT.md`，有取舍的决定留在 `docs/adr/`，界面结构以高保真定稿为准。这里不重复记录。

## 1. 当前阶段目标

<!--
写一条端到端的用户价值，不要列一批功能。
可用："用户能配置一个数据源并跑通一次同步"
不可用："做完数据源、同步、日志三个模块"

判据：说不出「用户从哪进来、做完什么、看到什么结果」，那它还是一批功能。
-->

## 2. 切片清单

<!--
四列都要填。某一列填不出来，通常说明切法有问题。

- 阻塞边：只写真正挡住当前切片的依赖。先后顺序不等于依赖
- 端到端能验证什么：一句话，用户视角。说不出来就不是切片
- 本片对应的高保真屏：定稿里的具体路径。这一列是进 task 时填 implement.jsonl 的取值范围，
  空着时，实现期子 agent 就没有结构依据
-->

| # | 切片 | 阻塞边 | 端到端能验证什么 | 本片对应的高保真屏 |
|---|---|---|---|---|
| 1 |  | 无 |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |

<!-- 宽重构（影响面铺满全仓的机械改动）走 expand–contract，第四列通常为空。见 skill 的 references/wide-refactor.md -->

## 3. frontier

<!--
frontier 指阻塞边已经全部完成、现在可以开工的切片。
每完成一片就重算一次。不要按表格顺序往下数，否则会漏掉排在后面但已经可以开工的切片。
-->

现在可开工：

- [ ] #_ ——

仍在等待：

| 片 | 在等谁 |
|---|---|
|  |  |

已完成：

- [x] #_ ——

---

## 待确认

<!-- Quiz 两轮后仍有分歧就记在这里，不再开第三轮。四字段：现象 / 判定 / 动作 / 落点 -->

| 现象 | 判定 | 动作 | 落点 |
|---|---|---|---|
|  |  |  |  |
