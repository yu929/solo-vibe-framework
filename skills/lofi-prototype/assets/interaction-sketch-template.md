<!--
填写须知（生成后删除本注释块）

非 UI 产品（CLI、API 服务、SDK、数据管道、嵌入式、AI agent）的低保真骨架。
UI 产品用 wireframe-base.html，不用本模板。

1. 覆盖面 = **当前切片** `prd.md` 声明的全部交互序列，含它自己的辅助命令和异常路径。
   不是只写主链路——漏一条，实现时就多一条由 AI 自由发挥。
   也**不需要**覆盖后面几片：那些需求还没被 brainstorm 收敛过，现在写等于替它们猜。
2. 每个方案单独一节，方案之间必须有结构性区别（命令组织方式、参数传递方式、
   同步 vs 异步、错误暴露位置），不是同一套接口换个名字。
3. 输出要「能读着走查」：命令连着真实感的输入输出，不是接口签名清单。
   看到 `--dry-run` 输出长什么样，人才能判断这个交互对不对。
4. 保真度红线同 ../references/fidelity-contract.md：不写实现、不定内部数据结构、
   不出数据库设计。只锁交互面的形状和顺序。
5. 假设与空白用 [假设] / [待确认] / [设计决策] 标出来，不要静默补默认值。
-->

# {产品名} · 交互面骨架

> **切片**: `{task-slug}`（`.trellis/tasks/{task-dir}/prd.md`）
> **产品形态**: 无 UI（{CLI / API 服务 / SDK / 数据管道 / …}）
> **定稿方案**: {走查选案后填；未选案时写「待选案」}

---

## 方案对比总览

| 方案 | 一句话取舍 |
|---|---|
| A | {牺牲什么换什么} |
| B | {…} |

<!-- 先让人读这张表筛方向，再往下读细节。2 个方案够用时不必凑 3 个。 -->

---

## 方案 A：{方案名}

**核心取舍**：{一句话。例：把配置和执行拆成两条命令，步骤清晰但要记两个命令}

### A.1 主链路

<!-- 每个序列：触发 → 真实感的输入 → 真实感的输出 → 状态变化。
     输出要写得像真的，包括进度提示、耗时、条数。 -->

#### 序列 1：{序列名}

```console
$ {命令示例 --带真实参数}
{像样的输出：进度、结果、耗时}
{…}
```

**说明**：{这一步做了什么，用户下一步该干什么}

#### 序列 2：{序列名}

```console
$ {…}
{…}
```

### A.2 辅助命令

<!-- 最常被漏的一类。列表/查看/删除/重跑/导出这些没有它们产品不可用。 -->

#### 序列 3：{序列名}

```console
$ {…}
{…}
```

### A.3 异常路径

<!-- 第二常被漏、也最贵的一类。每种失败要给不同的下一步，
     不要所有错误都输出一句「操作失败」。 -->

#### 异常 1：{参数错误}

```console
$ {带错误参数的命令}
{错误输出：说清哪里错了、正确格式是什么}
{退出码}
```

#### 异常 2：{认证失败}

```console
$ {…}
{…}
```

#### 异常 3：{部分成功}

```console
$ {…}
{说清成功多少、失败多少、失败明细在哪、怎么只重跑失败部分}
```

#### 异常 4：{超时 / 重试}

```console
$ {…}
{…}
```

#### 异常 5：{幂等重复提交}

```console
$ {重复执行同一个操作}
{是拒绝、是幂等返回原结果、还是新建一份？这里必须表态}
```

### A.4 状态迁移

<!-- 有状态的对象（任务、作业、连接、会话）必须画一张迁移表或 mermaid。
     只写"有这些状态"不够，要写"什么事件把它从哪推到哪"。 -->

| 当前状态 | 事件 | 下一状态 | 副作用 |
|---|---|---|---|
| {未配置} | {配置完成} | {待执行} | {无} |
| {待执行} | {触发} | {执行中} | {写审计日志} |
| {执行中} | {全部成功} | {已完成} | {…} |
| {执行中} | {部分失败} | {部分成功} | {保留失败明细} |
| {执行中} | {超时} | {失败} | {…} |

### A.5 契约要点

<!-- 只写交互面的形状，不写实现。 -->

| 项 | 内容 |
|---|---|
| 调用入口 | {命令 / 端点 / SDK 方法} |
| 必需参数 | {…} |
| 输出格式 | {人可读 / JSON / 两种都给（`--json`）} |
| 退出码约定 | {0 成功 / 1 参数错 / 2 认证失败 / 3 部分成功 / …} |
| 幂等性 | {是否幂等；重复调用会怎样} |
| 凭据传入方式 · CLI | {stdin / 权限受控的配置文件（0600）/ 环境注入 / 系统凭据存储}。**禁止走命令行参数** |
| 凭据传入方式 · API·SDK | {请求头 / SDK 构造参数}。**不进 URL 与 query string** |
| 凭据在输出里 | {日志、错误对象、`--verbose`、崩溃堆栈一律脱敏成 `***`；写清哪几处要脱敏} |

<!-- 「禁止走命令行参数」不是保守，是这条要求物理上无法与「不进历史和日志」同时成立：
     argv 会进 shell history、`ps` 的进程列表、容器/CI 的命令回显、以及崩溃诊断。
     写成"环境变量/配置文件/参数三选一，但别进历史"，等于给出一个选了就违约的选项。
     API/SDK 单列是因为它那边"参数"是合法的（构造函数入参不进 argv），别把两边混成一行。 -->

---

## 方案 B：{方案名}

**核心取舍**：{…}

<!-- 与方案 A 同结构：B.1 主链路 / B.2 辅助命令 / B.3 异常路径 / B.4 状态迁移 / B.5 契约要点。
     内容必须真的不同——如果 B 只是把 A 的命令改个名，就不算一个方案。 -->

---

## 定稿

<!-- 走查选案后填。落选方案保留在本文件里供回看，但不再是任何东西的源真。

     ⚠ 选案之后还有一步，别漏：把**只含选中方案**的内容另存为
       docs/discovery/wireframe/{task-slug}/final/interaction-sketch.md

     为什么不能就地标一下算数：后面切片的一致性检查只扫 */final/。
     定稿留在本文件里的话，第二个 CLI/API 切片读不到这一片定下的命令组织、
     错误格式、退出码和幂等契约——而这些恰恰是没人能凭印象发现不一致的东西。
     final/ 里也不要放全部方案，那样下一片不知道该沿用哪个。 -->

- **选定方案**：{A / B}
- **理由**：{一句话}
- **从落选方案合并进来的要素**：{具体是什么；没有就写「无」}
- **定稿副本**：`docs/discovery/wireframe/{task-slug}/final/interaction-sketch.md`
- **详细走查记录**：`docs/discovery/wireframe/{task-slug}/walkthrough-notes.md`
