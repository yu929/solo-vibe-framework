<!--
填写须知（生成后删除本注释块）

1. 本文件两段：走查发现（4 字段）+ 方案对比与选案留痕。两段都不能省。
2. 走查发现只 4 个字段，刻意不套 design-review 的 9 字段 ledger。
   理由：那套证据门槛（可定位设计依据 / 触发条件 / 受影响需求）是为设计准入设计的。
   原型走查阶段的发现形态是「这步太绕」「失败了不知道该干什么」——
   套九字段等于要求用户为每条直觉反馈写举证材料，会直接把走查变成新的审计负担。
3. 「落点」必须是一个具体位置（`prd.md` 某节 / 某屏 / `brief.md` 切片清单第几行），不能写「已知悉」。
   没有落点的发现等于没记。
4. 走查最多 2 轮。第 2 轮之后仍有分歧的，判定写「待确认」或「挪到后面的切片」，不进第 3 轮。
5. 方案对比段是走多方案路线必须付的记账成本。不写的话，下一个 session
   （或三周后的自己）会从零把方案讨论一遍。
-->

# {产品名} · 原型走查记录

> **切片**: `{task-slug}`（`.trellis/tasks/{task-dir}/prd.md`）
> **原型版本**: `docs/discovery/wireframe/{task-slug}/` @ {git 短 hash 或日期}
> **走查日期**: {YYYY-MM-DD}
> **走查轮次**: 第 {1|2} 轮 / 上限 2 轮

---

## 一、走查发现

<!-- 判定取值只有四个：
     改 prd         —— 这一片的需求要变（流程、状态、规则）
     改原型         —— 需求没变，只是结构或表达不对
     挪到后面的切片 —— 这事该做，但不是这一片
     待确认         —— 需要别人拍板或需要更多信息

     「挪到后面的切片」是逐片模式的关键出口。没有它，每片都会「顺手多做一点」，
     三片之后就回到了一次做全。 -->

| # | 现象 | 判定 | 动作 | 落点 |
|---|---|---|---|---|
| W1 | {走查时看到/感觉到什么。用大白话原样记，不要翻译成术语} | 改 prd | {具体改什么} | 本片 `prd.md` 屏清单 |
| W2 | {…} | 改原型 | {具体改什么} | 屏 4 字段映射 |
| W3 | {…} | 挪到后面的切片 | {加进切片清单并写依赖} | `brief.md` 切片清单 |
| W4 | {…} | 待确认 | {谁来定 / 什么时候必须定} | 本片 `prd.md` 待确认 Q4 |

### 本轮统计

| 判定 | 条数 |
|---|---|
| 改 prd | {n} |
| 改原型 | {n} |
| 挪到后面的切片 | {n} |
| 待确认 | {n} |

<!-- 第 2 轮时补一句停止判定：
     「第 2 轮新增『改 prd』{n} 条；已达轮次上限，剩余分歧转待确认/挪到后面的切片，进入回写。」-->

---

## 二、方案对比与选案

### 各方案取舍

| 方案 | 一句话取舍 | 走查中的实际表现 |
|---|---|---|
| A | {牺牲什么换什么} | {走的时候顺不顺；卡在哪} |
| B | {…} | {…} |
| C | {…} | {…} |

### 选案

- **选定**：方案 {X}
- **理由**：{一句话说清为什么。不要写"综合考虑"，写具体的那个决定性因素}
- **从落选方案合并进来的要素**：
  - {来自方案 Y 的某个具体做法} —— {为什么值得合并}
  - {…}
- **明确放弃的要素**：
  - {某个方案里挺好但决定不要的东西} —— {为什么不要}

<!-- 「明确放弃」这一栏是防复发的：不写下来的话，同一个想法会在下次讨论时
     以「我们当时是不是考虑过 X」的形式重新占用一轮。 -->

- **定稿落点**：`docs/discovery/wireframe/{task-slug}/final/` —— 有 UI 是 `final/index.html`，非 UI 是 `final/interaction-sketch.md`（**只含选中方案**；落选段落留在上一层的 `interaction-sketch.md`）

---

## 三、回写清单

<!-- 逐条勾掉，确认走查结论真的进了 prd——不回写的话，
     实现时读到的还是走查之前的旧共识。 -->

**回本片 `prd.md`**（必做）：

- [ ] 屏 / 序列清单已增删到与定稿一致
- [ ] 「必须表达的状态」已增删
- [ ] 待确认项：本轮定掉的已划掉，新暴露的已补上
- [ ] 已写明「页面结构与区域分组沿用 `wireframe/{task-slug}/final/`」

**回 `brief.md`**（只在触发时做，多数轮次为空）：

- [ ] 判定为「挪到后面的切片」的，已加进切片清单并标依赖
- [ ] 这一片其实不该这么切 → 切片清单已重排并写明理由
- [ ] 走查中裁决的术语已进措辞一节
- [ ] 阶段目标被证伪 → 已改并说明

**最后**：

- [ ] 单片规模警报（本片 > 6 屏）如触发，复核结论已留痕
- [ ] 材料已交给 planning summary

> **本文件不置任何 approved 标记。** 批准由用户在 Trellis 的 planning summary 那一步做
> （brainstorm 规定：没有用户显式批准就不许 `task.py start`）。
> 再加一个标记就是同一事实两处维护。
